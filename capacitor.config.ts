import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lab_list_view',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
